# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty — no source, build config, or version control yet.
There is no architecture or command set to document.

Once the project is scaffolded, replace this section with:
- **Commands**: build, lint, test (including how to run a single test), and local dev startup.
- **Architecture**: the cross-file structure and data flow that isn't obvious from reading any one file.
